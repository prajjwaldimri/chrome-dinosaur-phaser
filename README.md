# chrome-dinosaur-phaser

Chrome's Dinosaur Game implemented in Phaser

## Installing and Running

- First use yarn to install all the dependencies

> `yarn`

- Then to run the game execute the following command

> `yarn run start:dev`
